using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// A script that literally does nothing. We just need it for its MonoBehaviour ability
// to hook into the engine's coroutine system.
//
// MonoBehaviour isn't an abstract class, but we're still going to go with the decision
// to make a dedicated do-nothing class.
public class DoNothing : MonoBehaviour
{
}
